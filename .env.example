PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=parqueo_privados_gt

# JWT — Autor: Elmer Fuentes
# Sin sufijo: minutos. También se aceptan segundos (15s), minutos (30m) y horas (8h).
# JWT_LOGIN_MINUTES   = duración del refreshToken (tipo "login"): 7 días -> 168h
# JWT_SESSION_MINUTES = duración del accessToken (tipo "session"): 15 segundos -> 15s
JWT_SECRET=ParqueoPrivadosGT_Cambiar_Este_Secreto_2026
JWT_LOGIN_MINUTES=168h
JWT_SESSION_MINUTES=15s
